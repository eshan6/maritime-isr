#!/usr/bin/env bash
# Unit 0.2 — ESA SNAP install on the 24 GB Oracle VM (stub; expanded in 0.2 session).
# Placeholder so the infra path exists in Phase 0. The 0.2 session fills the
# esa-snap download + pyroSAR gpt configuration + memory caps for 24 GB RAM.
set -euo pipefail
echo "SNAP install is implemented in unit 0.2. Placeholder only."
exit 0
