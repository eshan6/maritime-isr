# After uploading to GitHub, rename these two files back to dotfiles:
#
#   gitignore.txt      ->  .gitignore
#   env.example.txt    ->  .env.example
#
# In GitHub web UI: open the file, click the pencil (Edit), and change the name
# in the path box at the top to the dotted name, then commit.
#
# .gitignore is important — it keeps your future real .env (credentials),
# data/, and *.duckdb files out of the repo. Don't skip it.
#
# Once both are renamed, delete this file.
