# Shared core
numpy>=1.24
pandas>=2.0
pyarrow>=15.0
scipy>=1.11
h3>=4.0

# Live-data path (execution-spec units 0.0-0.5)
pydantic>=2.6
duckdb>=0.10
# duckdb needs pytz to bind timezone-aware datetimes as query
# parameters against TIMESTAMPTZ columns (the registry as_of lookups).
pytz>=2024.1
requests>=2.31
websockets>=12.0
boto3>=1.34
shapely>=2.0

# Synthetic prototype (Phases 1-6)
scikit-image>=0.22
scikit-learn>=1.3
global-land-mask>=1.0

# SAR preprocessing on the VM (unit 0.2) — install with: pip install ".[sar]"
# pyroSAR>=0.25
# rasterio>=1.3

# Tests
pytest>=8.0
